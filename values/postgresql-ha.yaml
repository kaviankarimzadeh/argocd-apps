global:
  storageClass: "ceph-block"
  postgresql:
    username: "vault"
    database: "vault"
    repmgrUsername: "vault-rep"
    repmgrDatabase: "vault-rep"
    existingSecret: "postgresql-creds"
  pgpool:
    adminUsername: pgadmin
    existingSecret: "pgpool-creds"

persistence:
  enabled: true
  storageClass: "ceph-block"
  size: 1Gi