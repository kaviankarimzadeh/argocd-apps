global:
  storageClass: "ceph-block"
  postgresql:
    username: "vault"
    database: "vault"
    repmgr-username: "vault-rep"
    repmgr-database: "vault-rep"
    existingSecret: "postgresql-creds"
  pgpool:
    admin-username: pgadmin
    existingSecret: "pgpool-creds"

persistence:
  enabled: true
  storageClass: "ceph-block"
  size: 1Gi